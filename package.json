{
  "name": "agent-subtitle-ext",
  "version": "0.11.0",
  "private": true,
  "packageManager": "pnpm@10.31.0",
  "description": "YouTube AI subtitle translator via any OpenAI-compatible agent",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "watch": "node esbuild.config.mjs --watch",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit --pretty false"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.38",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0"
  }
}
